@using KrokodyliWeb.Utils
@inject IModalService modals 


<div class="container">

    <div class="portfolio-item row justify-content-center">
        @foreach(var (img, index) in Images.IterateSliceWithIndices())
        {
            <div class="item selfie col-lg-3 col-md-4 col-6 col-sm justify-content-center">
                <img class="img-fluid" src="@(img.Source)" alt="@(img.Alt)" @onclick="()=>PreviewImage(index)">
            </div>
        }
    </div>
</div>
      


@code {
    [Parameter]
    public ListView<ImageDescriptor> Images { get; set; } = ListView<ImageDescriptor>.Empty;



    private void PreviewImage(int imageIndex)
    {
        modals.ShowImagePreview(imageIndex, Images.List);
    }

}
